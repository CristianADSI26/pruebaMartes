import { FiltroCotizacionPipe } from './filtro-cotizacion.pipe';

describe('FiltroCotizacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCotizacionPipe();
    expect(pipe).toBeTruthy();
  });
});
