import { FiltroInspeccionPipe } from './filtro-inspeccion.pipe';

describe('FiltroInspeccionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroInspeccionPipe();
    expect(pipe).toBeTruthy();
  });
});
